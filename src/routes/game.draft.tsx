// src/routes/game.draft.tsx
import { Draft } from "../pages/GamePlay";

// Path relative to the parent '/game' route
export const path = "draft";

const element = () => <Draft />;
export default element;
